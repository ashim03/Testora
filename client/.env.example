# Client environment configuration
# Copy this file to .env and adjust if needed.

# Leave empty when running through the Vite dev proxy (defaults to "/api").
# When serving the built app separately, point this at the API origin.
VITE_API_URL=
